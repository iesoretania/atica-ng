{% trans_default_domain 'user' %}
{% extends 'generic/list.html.twig' %}

{% block path_new %}{{ path('admin_user_form_new') }}{% endblock %}
{% block path_list %}{{ path('admin_user_list') }}{% endblock %}

{% block table_header %}
    <th class="col-sm-4">{{ 'header.name'|trans }}</th>
    <th class="col-sm-5">{{ 'header.username'|trans }}</th>
    <th class="col-sm-2">{{ 'header.profiles'|trans }}</th>
{% endblock %}

{% block table_body %}
            {% for user in users %}
                {% set url = path('admin_user_form_edit', {'id': user.id}) %}
                <tr class="clickable-row" data-href="{{ url }}">
                    <td>
                        <a href="{{ url }}">{{ user.firstName }} {{ user.lastName }}</a>
                    </td>
                    <td><a href="{{ url }}">
                            {{ user.userNameAndEmailAddress }}
                        </a></td>
                    <td>
                        {{ user.globalAdministrator ? 'role.global_administrator'|trans }}
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="4">{{ 'table.no_users'|trans }}</td>
                </tr>
            {% endfor %}
{% endblock %}
